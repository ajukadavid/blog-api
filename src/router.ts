import { Router } from 'express'

const router = Router()

router.get('/posts', () => {})
router.get('/post/:id', () => {})
router.put('/posts/:id', () => {})
router.post('/posts/:id', () => {})
router.delete('/posts/:id', () => {})
